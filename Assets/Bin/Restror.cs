using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Restror : MonoBehaviour
{
    // Start is called before the first frame update
    private Vector3 a;
    void Start()
    {
        var aNormalized = a.normalized;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
